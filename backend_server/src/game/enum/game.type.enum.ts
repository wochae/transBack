export enum GameType {
  FRIEND = 'friend',
  NORMAL = 'normal match',
  RANK = 'rank match',
}

export enum GameSpeed {
  NORMAL = 'normal',
  FAST = 'fast',
  FATSTER = 'faster',
}

export enum MapNumber {
  A = 'map 0',
  B = 'map 1',
  C = 'map 2',
}
